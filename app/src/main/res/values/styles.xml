<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <!--<item name="colorAccent">@android:color/white</item>-->

        <item name="android:textColor">@color/colorText</item>

        <item name="android:buttonStyle">@style/ButtonNoCaps</item>
    </style>


    <style name="ButtonNoCaps" parent="Widget.AppCompat.Button">
        <item name="android:textAllCaps">false</item>
    </style>

    <style name="TicketTextTop" parent="Widget.AppCompat.TextView.SpinnerItem">
        <item name="android:textColor">@color/colourTicketText</item>
        <item name="android:textSize">10dp</item>
        <item name="android:gravity">center</item>
    </style>


    <style name="TicketTextBody" parent="Widget.AppCompat.TextView.SpinnerItem">
        <item name="android:padding">2sp</item>
        <item name="android:textColor">#000000</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">13sp</item>
    </style>

    <style name="TicketTextBodyDark" parent="TicketTextBody">
        <item name="android:background">@color/colourTicketBackgroundDark</item>
    </style>

    <style name="TicketTextBodyLight" parent="TicketTextBody">
        <item name="android:background">@color/colourTicketBackgroundLight</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
